<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>database on 知行合一</title>
    <link>https://fabletang.github.io/en/tags/database/</link>
    <description>Recent content in database on 知行合一</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <copyright>Copyright © 2016-{year} Fable Tang. All Rights Reserved.</copyright>
    <lastBuildDate>Fri, 26 Jun 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://fabletang.github.io/en/tags/database/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>PostgreSQL可以做到Redis绝大部分功能</title>
      <link>https://fabletang.github.io/en/posts/2026-06-26-postgres-redis/</link>
      <pubDate>Fri, 26 Jun 2026 00:00:00 +0000</pubDate>
      
      <guid>https://fabletang.github.io/en/posts/2026-06-26-postgres-redis/</guid>
      <description>PostgreSQL可以做到Redis绝大部分功能 我之前用的是一套很典型的Web应用技术栈：
PostgreSQL负责持久化数据存储 Redis负责缓存、发布订阅以及后台任务处理 两个数据库，两个体系需要管理，也意味着多了两处故障风险点。
后来我意识到：PostgreSQL可以做到Redis能做的功能。
于是我彻底移除了Redis，迁移过程是这样的。
一、设置：我使用Redis的目的 在替换之前，Redis主要处理三件事：
1、缓存（使用率70%）
// Cache API responses data, _ := json.Marshal(user) rdb.Set(ctx, fmt.Sprintf(&amp;#34;user:%d&amp;#34;, id), data, 3600*time.Second) 2、发布订阅（使用率20%）
// Real-time notifications msg, _ := json.Marshal(map[string]interface{}{&amp;#34;userId&amp;#34;: userId, &amp;#34;message&amp;#34;: message}) rdb.Publish(ctx, &amp;#34;notifications&amp;#34;, msg) 3、后台消息队列（使用率10%）
// Using asynq (Redis-based task queue) task := asynq.NewTask(&amp;#34;send-email&amp;#34;, []byte(`{&amp;#34;to&amp;#34;:&amp;#34;user@example.com&amp;#34;,&amp;#34;subject&amp;#34;:&amp;#34;Hi&amp;#34;}`)) client.Enqueue(task) 痛点：
需要备份两个数据库,当然好的设计是redis不持久化,假定为不可靠. Redis使用内存（规模化时成本很高） Redis持久化机制……很复杂,而且也不可靠 Postgres和Redis之间还存在一次网络跳转开销 二、我为什么考虑替换Redis 原因一：成本
我的Redis配置：
AWS ElastiCache（2GB）：每月45美元 若扩容至 5GB，每月费用将增至110美元 PostgreSQL：
已付费使用RDS（20GB存储）：每月50美元 即便增加5GB数据流量：每月仅需0.5美元 节省成本： 每月约100美元
原因二：运行复杂性
使用 Redis：
Postgres backup ✅ Redis backup ❓ (RDB? AOF? Both?) Postgres monitoring ✅ Redis monitoring ❓ Postgres failover ✅ Redis Sentinel/Cluster ❓ 不使用Redis：
Postgres backup ✅ Postgres monitoring ✅ Postgres failover ✅ 系统依赖组件更少。
原因三：数据一致性
经典问题：
// Update database _, err := db.Exec(ctx, &amp;#34;UPDATE users SET name = $1 WHERE id = $2&amp;#34;, name, id) // Invalidate cache rdb.Del(ctx, fmt.Sprintf(&amp;#34;user:%d&amp;#34;, id)) // ⚠️ What if Redis is down? // ⚠️ What if this fails? // Now cache and DB are out of sync 在PostgreSQL中，这类问题通过事务即可解决。
三、PostgreSQL特性 1、使用非日志表进行缓存 Redis：
data, _ := json.Marshal(sessionData) rdb.Set(ctx, &amp;#34;session:abc123&amp;#34;, data, 3600*time.Second) PostgreSQL：
CREATE UNLOGGED TABLE cache ( key TEXT PRIMARY KEY, value JSONB NOT NULL, expires_at TIMESTAMPTZ NOT NULL ); CREATE INDEX idx_cache_expires ON cache(expires_at); 插入：
INSERT INTO cache (key, value, expires_at) VALUES ($1, $2, NOW() + INTERVAL &amp;#39;1 hour&amp;#39;) ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value, expires_at = EXCLUDED.expires_at; 读：
SELECT value FROM cache WHERE key = $1 AND expires_at &amp;gt; NOW(); 清理（定期运行）：
DELETE FROM cache WHERE expires_at &amp;lt; NOW(); 什么是非日志表？
跳过预写式日志（WAL） 写入性能大幅提升 崩溃后数据不保留（非常适合用作缓存！） 表现：
Redis SET: 0.05ms Postgres UNLOGGED INSERT: 0.08ms 用作缓存已经完全够用。
2、基于LISTEN或NOTIFY实现发布订阅功能 接下来就精彩了。
PostgreSQL具有原生的发布订阅功能，但大多数开发人员并不了解。
1）Redis的发布订阅功能
// Publisher msg, _ := json.Marshal(map[string]interface{}{&amp;#34;userId&amp;#34;: 123, &amp;#34;msg&amp;#34;: &amp;#34;Hello&amp;#34;}) rdb.Publish(ctx, &amp;#34;notifications&amp;#34;, msg) // Subscriber pubsub := rdb.Subscribe(ctx, &amp;#34;notifications&amp;#34;) defer pubsub.Close() ch := pubsub.Channel() for msg := range ch { fmt.Println(msg.Payload) } 2）PostgreSQL的发布订阅功能
-- Publisher NOTIFY notifications, &amp;#39;{&amp;#34;userId&amp;#34;: 123, &amp;#34;msg&amp;#34;: &amp;#34;Hello&amp;#34;}&amp;#39;; // Subscriber (Go with pgx) conn, _ := pgx.Connect(ctx, os.Getenv(&amp;#34;DATABASE_URL&amp;#34;)) defer conn.Close(ctx) _, err := conn.Exec(ctx, &amp;#34;LISTEN notifications&amp;#34;) for { notification, err := conn.WaitForNotification(ctx) if err != nil { log.Fatal(err) } fmt.Println(&amp;#34;Payload:&amp;#34;, notification.Payload) } 性能对比：
Redis pub/sub latency: 1-2ms Postgres NOTIFY latency: 2-5ms 性能略低，但优势明显：
无需额外部署中间件 可在事务中使用 可与查询语句结合使用 3）实际应用场景：实时日志追踪
在我的日志管理应用中，需要实现日志实时流式推送。
使用Redis：
// When new log arrives _, err := db.Exec(ctx, &amp;#34;INSERT INTO logs ...&amp;#34;) msg, _ := json.Marshal(logEntry) rdb.Publish(ctx, &amp;#34;logs:new&amp;#34;, msg) // Frontend listens pubsub := rdb.Subscribe(ctx, &amp;#34;logs:new&amp;#34;) ch := pubsub.Channel() 问题： 有两个操作，如果发布失败怎么办？
使用PostgreSQL：
CREATE FUNCTION notify_new_log() RETURNS TRIGGER AS $$ BEGIN PERFORM pg_notify(&amp;#39;logs_new&amp;#39;, row_to_json(NEW)::text); RETURN NEW; END; $$ LANGUAGE plpgsql; CREATE TRIGGER log_inserted AFTER INSERT ON logs FOR EACH ROW EXECUTE FUNCTION notify_new_log(); 现在整个操作是原子性的：插入数据与通知推送，要么同时生效，要么都不执行。
// Frontend (via SSE) func logsStreamHandler(w http.ResponseWriter, r *http.Request) { conn, err := pool.Acquire(r.Context()) if err != nil { http.Error(w, err.Error(), http.StatusInternalServerError) return } defer conn.Release() w.Header().Set(&amp;#34;Content-Type&amp;#34;, &amp;#34;text/event-stream&amp;#34;) w.Header().Set(&amp;#34;Cache-Control&amp;#34;, &amp;#34;no-cache&amp;#34;) w.Header().Set(&amp;#34;Connection&amp;#34;, &amp;#34;keep-alive&amp;#34;) conn.Exec(r.Context(), &amp;#34;LISTEN logs_new&amp;#34;) for { notification, err := conn.Conn().WaitForNotification(r.Context()) if err != nil { break } fmt.Fprintf(w, &amp;#34;data: %s\n\n&amp;#34;, notification.Payload) w.(http.Flusher).Flush() } } 结果： 无需Redis即可实现实时日志流传输。
3、基于SKIP LOCKED实现任务队列 Redis（使用asynq）：
task := asynq.NewTask(&amp;#34;send-email&amp;#34;, []byte(`{&amp;#34;to&amp;#34;:&amp;#34;user@example.com&amp;#34;,&amp;#34;subject&amp;#34;:&amp;#34;Hi&amp;#34;}`)) client.Enqueue(task) // Worker mux.HandleFunc(&amp;#34;send-email&amp;#34;, func(ctx context.Context, t *asynq.Task) error { var payload EmailPayload json.Unmarshal(t.Payload(), &amp;amp;payload) return sendEmail(payload) }) PostgreSQL：
CREATE TABLE jobs ( id BIGSERIAL PRIMARY KEY, queue TEXT NOT NULL, payload JSONB NOT NULL, attempts INT DEFAULT 0, max_attempts INT DEFAULT 3, scheduled_at TIMESTAMPTZ DEFAULT NOW(), created_at TIMESTAMPTZ DEFAULT NOW() ); CREATE INDEX idx_jobs_queue ON jobs(queue, scheduled_at) WHERE attempts &amp;lt; max_attempts; 入队：</description>
    </item>
    
  </channel>
</rss>

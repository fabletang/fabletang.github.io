<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>vpn on 知行合一</title>
    <link>https://fabletang.github.io/en/tags/vpn/</link>
    <description>Recent content in vpn on 知行合一</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <copyright>Copyright © 2016-{year} Fable Tang. All Rights Reserved.</copyright>
    <lastBuildDate>Thu, 13 Aug 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://fabletang.github.io/en/tags/vpn/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Netbird 私有网络搭建完整指南</title>
      <link>https://fabletang.github.io/en/posts/2026-08-13-netbird_private_network_a_s_w_v2/</link>
      <pubDate>Thu, 13 Aug 2026 00:00:00 +0000</pubDate>
      
      <guid>https://fabletang.github.io/en/posts/2026-08-13-netbird_private_network_a_s_w_v2/</guid>
      <description>Netbird 私有网络搭建完整指南 1. 架构概述 本方案通过在三台设备上部署 Netbird，组建一个安全的私有网状网络：
设备 角色 网络状况 服务器 S (Linux, 有公网IP) 控制平面 + 中继节点 部署 Netbird 管理服务、信令服务和中继服务 服务器 A (Ubuntu, 内网) 目标服务端 安装 Netbird 客户端，加入私有网络 工作站 W (macOS, 异地) 客户端 安装 Netbird 客户端，加入私有网络 最终效果：
工作站 W 可通过 netbird ssh 或原生 SSH 客户端访问服务器 A 服务器 A 上 8080 端口的 Web 服务可通过 netbird expose 命令生成公网可访问的 URL Netbird 是基于 WireGuard 的开源点对点 VPN 平台，能在分布式基础设施间建立加密的网状网络，无需手动配置防火墙或端口转发。
2. 前提条件 2.1 硬件要求 控制平面服务器 S：至少 1 CPU、2GB 内存 中继服务器（可与控制平面共用）：至少 1 CPU、1GB 内存 服务器 A 和工作站 W：无特殊硬件要求 2.2 软件要求 服务器 S 和 A 均运行 Ubuntu（推荐 20.04 LTS 或更高版本） 服务器 S 需安装 Docker 和 Docker Compose 服务器 S 需有一个域名（用于 TLS 证书），DNS A 记录指向其公网 IP 2.3 防火墙端口要求 在服务器 S（控制平面）上开放以下端口：
端口 协议 用途 80 TCP HTTP（重定向到 HTTPS） 443 TCP HTTPS（控制台、管理 API、信令、中继） 33073 TCP 管理服务 gRPC 10000 TCP 管理服务 HTTP 33080 TCP 中继（WebSocket/QUIC） 3478 UDP STUN 服务 49152-65535 UDP TURN 端口范围 在服务器 A 和工作站 W 上： 无需开放任何入站端口。
3. 部署 Netbird 控制平面（在服务器 S 上） 3.1 安装 Docker 和 Docker Compose # 更新软件包索引 sudo apt update # 安装依赖 sudo apt install ca-certificates curl gnupg lsb-release -y # 添加 Docker 官方 GPG 密钥 sudo install -m 0755 -d /etc/apt/keyrings curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg # 添加 Docker 仓库 echo &amp;#34;deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable&amp;#34; | sudo tee /etc/apt/sources.list.d/docker.list &amp;gt; /dev/null # 安装 Docker Engine 和 Compose 插件 sudo apt update sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y 3.2 部署 Netbird 控制平面 # 创建 Netbird 目录 mkdir -p ~/netbird &amp;amp;&amp;amp; cd ~/netbird # 下载官方快速启动脚本 curl -fsSL https://pkgs.netbird.io/selfhosted/install.sh | bash 脚本运行过程中会提示你输入域名（如 netbird.your-domain.com），请确保该域名的 DNS A 记录已指向服务器 S 的公网 IP。
3.3 验证控制平面运行状态 docker compose ps 应看到以下服务均处于 Running 状态：
management signal relay coturn dashboard zitadel（身份提供商） 4. 配置中继服务（在服务器 S 上） 如果控制平面已包含中继服务，可跳过此步。但如果需要将中继服务独立部署或重新配置，请按以下步骤操作。
4.1 生成认证密钥 openssl rand -base64 32 保存输出的密钥，后续步骤需要用到。
4.2 创建中继配置 mkdir -p ~/netbird-relay &amp;amp;&amp;amp; cd ~/netbird-relay 创建 relay.env 文件：
NB_LOG_LEVEL=info NB_LISTEN_ADDRESS=:443 NB_EXPOSED_ADDRESS=rels://relay.your-domain.com:443 NB_AUTH_SECRET=你生成的密钥 NB_LETSENCRYPT_DOMAINS=relay.your-domain.com NB_LETSENCRYPT_EMAIL=your-email@example.com NB_LETSENCRYPT_DATA_DIR=/data/letsencrypt NB_ENABLE_STUN=true NB_STUN_PORTS=3478 注意：将 relay.your-domain.com 替换为你的中继域名，your-email@example.com 替换为你的邮箱。
4.3 创建 docker-compose.yml services: relay: image: netbirdio/relay:latest container_name: netbird-relay restart: unless-stopped ports: - &amp;#39;443:443&amp;#39; - &amp;#39;3478:3478/udp&amp;#39; env_file: - relay.env volumes: - relay_data:/data logging: driver: &amp;#34;json-file&amp;#34; options: max-size: &amp;#34;500m&amp;#34; max-file: &amp;#34;2&amp;#34; volumes: relay_data: 4.4 启动中继服务 docker compose up -d 4.5 更新主服务器配置 在控制平面的 management.json 中添加中继服务器配置，然后重启管理服务：
cd ~/netbird docker compose restart management 5. 安装 Netbird 客户端并加入网络 5.1 在服务器 A（Ubuntu）上安装 方式一：官方安装脚本
curl -fsSL https://pkgs.netbird.io/install.sh | sh 方式二：APT 方式安装
# 添加仓库 sudo apt-get update sudo apt-get install ca-certificates curl gnupg -y curl -sSL https://pkgs.netbird.io/debian/public.key | sudo gpg --dearmor --output /usr/share/keyrings/netbird-archive-keyring.gpg echo &amp;#39;deb [signed-by=/usr/share/keyrings/netbird-archive-keyring.gpg] https://pkgs.netbird.io/debian stable main&amp;#39; | sudo tee /etc/apt/sources.list.d/netbird.list # 安装 sudo apt-get update sudo apt-get install netbird -y 5.2 在工作站 W（macOS）上安装 方式一：下载安装包
访问 Netbird 官方下载页 下载 macOS 版本的 .pkg 安装包 双击安装并按照提示完成 方式二：使用 Homebrew
brew install netbirdio/tap/netbird 5.3 加入 Netbird 网络 在服务器 A 上：
sudo netbird up 在工作站 W 上：
netbird up 首次运行会输出一个登录 URL，在浏览器中打开并完成身份验证（使用你在控制平面配置的身份提供商，如 Zitadel）。
5.4 验证连接 netbird status 应能看到所有已连接的设备及其分配的 IP 地址（格式为 100.x.x.x）。
6. 配置 SSH 访问（工作站 W → 服务器 A） Netbird 提供了内置的 SSH 服务器，无需暴露端口 22 到公网。
6.1 在服务器 A 上启用 SSH 服务器 sudo netbird up --allow-server-ssh 6.2 在 Netbird 控制台中启用 SSH 登录 Netbird 控制台（https://netbird.your-domain.com） 进入 Peers → 选择服务器 A 将 SSH Access 开关设置为 ON 6.</description>
    </item>
    
    <item>
      <title>Tailscale 私有网络方案：A 内网服务器 &#43; S 公网 Gateway &#43; W 异地工作站</title>
      <link>https://fabletang.github.io/en/posts/2026-08-13-tailscale_private_network_a_s_w_v2/</link>
      <pubDate>Wed, 12 Aug 2026 00:00:00 +0000</pubDate>
      
      <guid>https://fabletang.github.io/en/posts/2026-08-13-tailscale_private_network_a_s_w_v2/</guid>
      <description>目标：A 不开放公网入站端口；W 可以通过 Tailscale 访问 A；同时允许非 Tailscale 用户通过公网访问 A 的 8080 Web 服务。 1. 网络环境 有三台服务器/工作站：
设备 系统 网络 作用 A Ubuntu 内网，防火墙禁止公网入站，但可访问 Internet 内部业务服务器 S Linux 固定公网 IP，可访问 Internet 公网 Web Gateway / 反向代理 W macOS 异地网络，可访问 Internet 运维工作站 需求：
W 可以 SSH 到 A。 W 可以访问 A 的 8080 Web 服务。 A 不开放公网 22。 A 不开放公网 8080。 A 不需要端口映射。 其他非 Tailscale 用户也可以访问 A 的 Web 8080 服务。 W 启用 Tailscale 后，仍然正常访问普通 Internet。 S 有固定公网 IP，可以作为公网 Web 入口。 2. 最终推荐架构 这里建议把 S 定位为：
公网 Web Gateway / Reverse Proxy
而不是传统的 VPN Server。
Internet │ ┌─────────────┴─────────────┐ │ │ ▼ ▼ macOS W 普通 Internet 用户 Tailscale 成员 非成员 │ │ │ Tailscale │ HTTPS :443 │ │ ▼ ▼ ┌─────────────┐ ┌─────────────┐ │ Ubuntu A │◄─────────────│ Linux S │ │ 内网服务器 │ Tailscale │ 公网Gateway │ │ │ │ 固定公网 IP │ └─────────────┘ └─────────────┘ │ │ └────── Web :8080 ─────────┘ 实际访问关系：
W │ │ Tailscale ├──────────────────────► A:22 │ └──────────────────────► A:8080 普通 Internet 用户 │ │ HTTPS ▼ S:443 │ │ Tailscale ▼ A:8080 因此：
A 公网 TCP/22 = 不开放 A 公网 TCP/8080 = 不开放 S 公网 TCP/443 = 开放 S → A = Tailscale 私有网络 W → A = Tailscale 私有网络 这是本场景最推荐的生产架构。
3. 为什么 S 不应该作为传统 VPN Server 传统方案：
W │ │ WireGuard ▼ S │ │ NAT / Routing ▼ A 需要维护：
WireGuard UDP 监听端口 IP forwarding iptables/nftables NAT 路由 客户端密钥 返回路径 而使用 Tailscale：
W ──────────┐ │ S ──────────┼── Tailnet │ A ──────────┘ A、S、W 都成为 Tailnet 节点。
S 不需要作为 VPN Server。
S 只负责：
公网 HTTPS │ ▼ Reverse Proxy │ ▼ Tailscale │ ▼ A:8080 4. Tailscale 网络 建议三台机器都加入同一个 Tailnet：
A = 100.x.x.10 S = 100.x.x.20 W = 100.x.x.30 这些只是示例地址，实际地址由 Tailscale 分配。
拓扑：
Tailnet │ ┌─────────┼─────────┐ │ │ │ ▼ ▼ ▼ A S W 100.x.10 100.x.20 100.x.30 Tailscale 节点之间优先尝试建立直接连接；如果网络条件不允许，也可以使用 Tailscale 的中继机制。
5. A 安装 Tailscale Ubuntu A：
curl -fsSL https://tailscale.com/install.sh | sh 启动：
sudo systemctl enable --now tailscaled 登录：
sudo tailscale up 查看状态：
tailscale status 查看 Tailscale IP：
tailscale ip 例如：
100.64.10.10 6. S 安装 Tailscale S：
curl -fsSL https://tailscale.com/install.sh | sh 启动：
sudo systemctl enable --now tailscaled 登录：
sudo tailscale up 检查：
tailscale status 例如：
A 100.64.10.10 S 100.64.10.20 7. W 安装 Tailscale macOS W 安装 Tailscale 客户端。
登录同一个 Tailnet。
确认：
tailscale status 应该可以看到 A：
100.64.10.10 server-a 8. W SSH 到 A 最简单：
ssh ubuntu@100.64.10.10 如果启用了 MagicDNS：
ssh ubuntu@server-a 流量：
W │ │ Tailscale ▼ A:22 A 不需要开放公网 TCP/22。
Tailscale 官方文档也支持直接通过 Tailscale IP 或 MagicDNS 主机名使用普通 SSH。citeturn0search1
9. SSH 推荐：先使用普通 OpenSSH 建议第一阶段：
普通 OpenSSH + Tailscale 网络 例如：
ssh ubuntu@server-a 这样 A 上原来的：
/etc/ssh/sshd_config ~/.ssh/authorized_keys 全部可以继续使用。
这是改造风险最低的方式。
10. 可选：Tailscale SSH 如果希望由 Tailscale 管理 SSH 身份认证和授权，可以使用：
sudo tailscale set --ssh 然后使用：
ssh ubuntu@server-a Tailscale SSH 可以利用 Tailnet 的访问控制策略管理 SSH 权限。官方文档说明，启用后 Tailscale 会接管来自 Tailnet 的目标端口 22，而不会修改原来的 sshd_config 和 authorized_keys。citeturn0search0
对于已有生产环境，建议：
第一阶段： 普通 SSH + Tailscale 第二阶段： 根据权限管理需求考虑 Tailscale SSH 11. A 的 8080 Web 服务 假设 A：
Web Application │ ▼ 127.0.0.1:8080 例如：
curl http://127.0.0.1:8080 正常。
现在有两个需求：
W W → A:8080 普通用户 Internet → S:443 → A:8080 因此推荐不要让 A 直接暴露公网 8080。</description>
    </item>
    
  </channel>
</rss>
